# プロジェクト概要

Claude Codeを用いた**ウォーターフォール型開発の練習**プロジェクト。
題材: Windows GUIで動作する五目並べ(Gomoku)ゲームをPythonで開発する。

## 開発プロセス

以下の順に工程を進める。各工程の成果物はユーザーのレビュー・承認を得てから次工程に進むこと（勝手に次工程へ進まない）。

1. 要件定義 (`docs/01_requirements/`)
2. コンポーネント設計 (`docs/02_component_design/`)
3. 関数設計 (`docs/03_function_design/`)
4. 実装 (`src/`)
5. テスト (`docs/04_test/` に仕様書、`tests/` にテストコード)

各工程の成果物には、要件定義書のID（REQ-xx / NFR-xx / CON-xx）を起点としたIDを付与し、`docs/traceability_matrix.md` のトレーサビリティマトリックスに追記すること（詳細は「トレーサビリティマトリックス」章を参照）。

## 現在の進捗（2026-08-11時点）

- [x] フォルダ構成・ローカルGitリポジトリ作成済み
- [x] `docs/01_requirements/requirements.md` 作成済み（v1.1、誤字修正・長連の勝利明記・勝利ハイライト・NFR-05を反映）
- [x] ユーザーによる要件定義書（v1.1）のレビュー・承認 完了（2026-08-11、詳細は `docs/qa_log.md` No.4）
- [ ] **コンポーネント設計 未着手** ← 次はここから再開
- [ ] 関数設計
- [ ] 実装
- [ ] テスト

再開時は、`docs/02_component_design/` にコンポーネント設計書の作成に着手すること（要件定義の承認は完了済みのため再確認は不要）。あわせて `docs/traceability_matrix.md` を新規作成し、要件定義書のREQ-xx/NFR-xx/CON-xxを行として起こしておくこと（詳細は「トレーサビリティマトリックス」章を参照）。

## 確定した仕様（要件定義で合意済み）

- 言語: Python、GUIは標準ライブラリの `tkinter`（追加パッケージ不要）
- 対局モード: 人 対 人のみ（CPU対戦・ネットワーク対戦は対象外）
- 盤面: 15×15、勝利条件は5つ連続（五連）
- 先手: 黒石
- 構成管理: Git（ローカルリポジトリのみ、リモート無し）

## トレーサビリティマトリックス

要件から実装・テストまでの対応関係を追跡できるよう、`docs/traceability_matrix.md` を工程が進むたびに追記・更新する（工程ごとに別ファイルを作らず、1ファイルで育てていく）。

- 要件↔実装↔テストを1行にまとめた単一表ではなく、**隣接する工程どうしの対応表（1行=1ペア）を4つ**用意する方式とする: ①要件×コンポーネント、②コンポーネント×関数、③関数×テスト、④要件×テスト（直接検証トレース）
  - 理由: 1要件が複数コンポーネント/関数/テストに対応する場合や、逆に1つの関数・テストが複数要件にまたがる場合（多対多）があるため。1行=1要件の単一表ではセルに複数IDを詰め込むことになり、双方向の追跡が困難になる
  - 同じIDが複数行に登場してよい（一対多・多対一・多対多をすべて自然に表現できる）
- 各工程の成果物（設計書・実装・テスト仕様書）を作成・更新した際に、対応する対応表に行を追記すること
- コンポーネント設計・関数設計・テスト仕様書側でも、該当するIDをドキュメント内に明記すること（マトリックス側だけにIDを持たせない）
- テスト工程完了時には、「要件×テスト対応表」に全要件ID（REQ/NFR/CON）が最低1行登場していることを確認し、テストの網羅性チェックとすること
- マトリックスの更新自体は「変更操作の承認について」に従い、通常のドキュメント編集としてその都度の承認確認は不要

## 品質方針（テスト容易性）

開発者によるテスト（主に `tests/` での単体テスト）が行いやすいことを、コンポーネント設計・関数設計の段階から重視する。

- ゲームロジック（盤面状態・手番管理・勝敗判定など）とGUI（tkinter）表示処理を分離し、ロジック層はtkinterに依存せず単体テスト可能な構成にすること
- 関数・メソッドは可能な限り入力と出力（戻り値）が明確な単位に分割し、副作用（画面描画・グローバル状態変更など）とロジックを混在させないこと
- 関数設計書には、各関数の入出力仕様をテストケース設計に転用できる粒度で記載すること
- テスト仕様書（`docs/04_test/`）の各テストケースは、対応する関数設計ID・要件IDとともにトレーサビリティマトリックスに反映すること

## 変更操作の承認について

プロジェクトフォルダ内の変更はGitで追跡・復元可能なため、以下の場合は事前の承認確認を省略してよい。

- 対象: ドキュメント・ソースコードの編集など、Gitで追跡可能な個別のファイル変更操作
- 対象: 上記の変更を確定させる `git commit`（Claude Codeのデフォルト動作は「ユーザーの明示的な指示がない限りコミットしない」だが、本プロジェクトではこれを上書きし、Gitで追跡・復元可能なコミットは都度の承認確認なしに実行してよい。コミットメッセージ規約は「Gitコミット規約」章に従うこと）
- 条件: ユーザーがその場で明示的に承認確認を求めていない場合

ただし、以下は引き続きユーザーの明示的な承認を得てから行うこと（対象外）。

- 「開発プロセス」章に定める各工程間のゲート承認（例: 要件定義→コンポーネント設計への移行など、次工程に進む判断）
- Gitで追跡されない・復元不能な操作（例: `git push`、`git reset --hard`、ファイル削除、外部システムへの送信など）

## 判断・承認のQA記録

Claudeがユーザーに判断や承認を仰ぎ、ユーザーが回答した場合は、その要点を `docs/qa_log.md` のQA表に追記すること（日付・工程・質問内容・回答内容を簡潔にまとめる）。

## Gitコミット規約

ユーザー自身のコミットとClaudeが作成したコミットを区別するため、Claudeがコミットする際は必ず以下のルールに従うこと。

- コミットメッセージの先頭に `[claude] ` を付ける
- コミット時に `--author="Claude <noreply@anthropic.com>"` を指定する（コミッターは変更しない）
- 例:
  ```powershell
  git commit -m "[claude] 要件定義書を作成" --author="Claude <noreply@anthropic.com>"
  ```
- ユーザー自身が手動でコミットする場合は、通常通りグローバルGit設定（ImSanghun / 21452104tpu@gmail.com）でコミットしてよい（接頭辞不要）

## 環境メモ

- このマシンには元々Gitが入っておらず、セッション中に `winget install --id Git.Git -e` でインストール済み。
- **重要**: このPowerShell環境ではツール呼び出しごとにシェル状態（PATH等）がリセットされる。
  `git` コマンドを実行する際、`git`が見つからない場合は以下でPATHを通してから実行すること。
  ```powershell
  [System.Environment]::SetEnvironmentVariable("PATH", [System.Environment]::GetEnvironmentVariable("PATH","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("PATH","User"), "Process")
  ```
- `git config user.email` / `user.name` はグローバル設定済み（21452104tpu@gmail.com / ImSanghun）。

## フォルダ構成

```
docs/
  01_requirements/       要件定義書
  02_component_design/   コンポーネント設計書（基本設計）
  03_function_design/    関数設計書（詳細設計）
  04_test/               テスト仕様書
  traceability_matrix.md トレーサビリティマトリックス（全工程共通・1ファイル）
src/                      実装コード
tests/                    テストコード
```
